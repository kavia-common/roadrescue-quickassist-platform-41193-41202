# RoadRescue – QuickAssist (User Website) environment variables
# CRA rule: only variables prefixed with REACT_APP_ are exposed to the browser.

# --- Supabase (required) ---
# Your project URL, like: https://<project-ref>.supabase.co
REACT_APP_SUPABASE_URL=

# Preferred: anon/public key (JWT string) from Supabase → Project Settings → API → anon/public
REACT_APP_SUPABASE_ANON_KEY=

# Legacy fallback supported by this app (some setups store anon key here)
# REACT_APP_SUPABASE_KEY=

# --- Optional app tuning ---
# REACT_APP_BOOT_TIMEOUT_MS=15000

# --- Twilio (DEMO ONLY; do not ship these in production) ---
# The demo component accepts both REACT_APP_TWILIO_* and a legacy double-prefixed variant.
# REACT_APP_TWILIO_ACCOUNT_SID=
# REACT_APP_TWILIO_AUTH_TOKEN=
# REACT_APP_TWILIO_FROM_NUMBER=
# REACT_APP_TWILIO_MESSAGING_SERVICE_SID=
